source 'https://rubygems.org'

gem 'rails', '2.3.18', require: nil

gem 'sqlite3', '1.3.13', require: 'sqlite3'
gem 'will_paginate', '2.3.16' # Don't use '3.x.x' # LOCKED DOWN
gem 'iconv'
gem 'faker'
gem 'overcommit'

group :development do
  gem 'rails-footnotes'
end

group :test do
  gem 'rspec'
end
